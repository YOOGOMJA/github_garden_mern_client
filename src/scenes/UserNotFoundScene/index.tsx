import React from 'react';
import { CommonStyles } from '../../styles' ;

const UserNotFoundScene = ()=>{
    return (<div style={ CommonStyles.container }>
        <p style={ CommonStyles.text }>user not found</p>
    </div>);
}

export default UserNotFoundScene;